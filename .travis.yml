language: python
python:
  - "3.8"

services: docker

before_script: pip install docker-compose

# command to run tests
script:
  - docker-compose run app sh -c "ls -l && python manage.py test && flake8"
